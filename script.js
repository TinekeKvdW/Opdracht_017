function calculateSquared(number1, number2) {
    const total = number1 * number1; number2 * number2;
    const calculateSquared = total + total;
    const sum = total * total;
    return result;
};
console.log(result);
/*
calculateSquared(5, 8);

const calculateSquared = function (number1, number2) {
    const total = number1 * number1; number2 * number2;
    const calculateSquared = total + total;
    const sum = total * total;
    return result;
};

calculateSquared(5, 8);

const calculateSquared = (number1, number2) => {
    const total = number1 * number1; number2 * number2;
    const calculateSquared = total + total;
    const sum = total * total;
    return result;
};
calculateSquared(5, 8); */